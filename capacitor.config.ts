import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alancheng.FriendlyTime',
  appName: 'friendly-time',
  webDir: 'dist'
};

export default config;
